/* import {assert, should , expect} from 'chai'
import chaiHttp from 'chai-http'
let chai = chaiModule.use(chaiHttp)

describe('api-test',()=>{
  it('get-user',(done)=>{
    chai.request(server)
    .get('/users')
    .end((err,response)=>{
      expect(response.status).to.be.equal(200)
    })
  })
}) */